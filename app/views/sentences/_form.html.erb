<%= form_for(@sentence) do |f| %>

  <% if @sentence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sentence.errors.count, "error") %> prohibited this sentence frombeing saved:</h2>

      <ul>
        <% @sentence.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :id %>:
    <%= @sentence.id %>
  </div>
  <div class="form-group">
    <%= f.label :english %><br>
    <%= f.text_field :english, size: 80, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :japanese %><br>
    <%= f.text_field :japanese, size: 80, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>

<% end %>
